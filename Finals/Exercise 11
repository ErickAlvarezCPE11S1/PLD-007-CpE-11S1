#include <Iostream>
#include <cstdlib>
using namespace std;

int main(){
	int* ptr=(int*)malloc(sizeof(int));
	if(ptr i != nullptr){
		*ptr=10;
		cout << "Value: "<< *ptr<<endl;
		free(ptr;)
	}
	
	return 0;
}
